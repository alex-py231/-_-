using namespace std;
#include <iostream>
#include "функции.h"
#define Omega 1.
int main()
{
    setlocale(LC_ALL, "Russian");

	/*int n;
	cin >> n;
	double** mat = new double* [n];
	double** mat1 = new double* [n];
	double* lambda = new double[n];
	double** sob_vec = new double* [n];
	for (int i = 0; i < n; i++)
	{
		mat[i] = new double[n];
		mat1[i] = new double[n];
		sob_vec[i] = new double[n];
		for (int j = 0; j < n; j++)
		{
			cin >> mat[i][j];
		}
	}
	m_QR(mat, n,lambda,sob_vec);
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < n; j++)
		{
			cout << sob_vec[i][j] << "   ";
		}
		cout << lambda[i];
		cout << endl;
	}*/
    return 0;
}